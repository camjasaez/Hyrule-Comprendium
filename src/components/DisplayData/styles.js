import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '100%',
  },
  loading: {
    width: '100%',
    height: '20xp',
  },
}));

export default useStyles;
